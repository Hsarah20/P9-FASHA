@isTest
private class UpdateAllAccountsSchedulerTest{

    @isTest
    static void testUpdateStatusTrip() {
        UpdateAllAccountsScheduler scheduler = new UpdateAllAccountsScheduler();
        scheduler.execute(null); 
    }
}